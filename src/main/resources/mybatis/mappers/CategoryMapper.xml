<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.team01.category.dao.CategoryDao">


    <select id="categoryList" resultType="CategoryVO">
        SELECT
            trim(LPAD(' ', LEVEL * 2) || cateName) AS cateNames,
            cateId,
            parentId,
            depthLevel,
            LEVEL,
            SUBSTR(SYS_CONNECT_BY_PATH(cateName, ' > '), 4) AS fullPath
        FROM
            categories
                START WITH parentId IS NULL
        CONNECT BY PRIOR cateId = parentId
        ORDER SIBLINGS BY cateName
    </select>

</mapper>