<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.team01.qna.dao.QnaOneDao">  <!--사용하는 Dao 연결-->
    <!--메서드 구현부-->
    <!--id로 특정 사용자 데이터 조회-->
    <select id="userQnaDataList" parameterType="String" resultType="QnaOneVO">
        SELECT * FROM qnaOne WHERE "clientId" = #{clientId}
    </select>
</mapper>


