<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        <mapper namespace="com.example.team01.board.oneboard.dao.OneBoardDao">

    <insert id="CreateOneBoard" parameterType="com.example.team01.vo.OneBoardVO">
        <selectKey keyProperty="qnaOneId" resultType="String" order="BEFORE">
            SELECT 'qnaone' || LPAD(ADDQNAONE_SEQ.NEXTVAL, 4, '0') FROM DUAL
        </selectKey>
        
        INSERT INTO qnaone (
            "qnaOneId",
            "qnaTitle",
            "qnaContent",
            "qnaWriter",
            "qnaDate",
            "qnaStatus",
            "clientId",
            "roleId",
            "attachmentID"
        ) VALUES (
            #{qnaOneId},
            #{qnaTitle},
            #{qnaContent},
            #{qnaWriter},
            SYSDATE,
            '답변 대기',
            #{clientId},
            'ROLE_CLIENT',
            #{attachmentID, jdbcType=VARCHAR}
        )
    </insert>

    <select id="GetOneBoardList" parameterType="String" resultType="com.example.team01.vo.OneBoardVO">
        <!-- SELECT * FROM qnaone WHERE "clientId" = #{userId} -->

        SELECT * FROM qnaone WHERE "clientId" = #{userId} AND "qnaDel" IS NULL OR "qnaDel" = 'N'
        ORDER BY "qnaOneId" DESC
    </select>

    <select id="getOneBoardDetail" parameterType="map" resultType="com.example.team01.vo.OneBoardVO">
        SELECT "qnaOneId",
        "qnaTitle",
        "qnaContent",
        "qnaWriter",
        "qnaDate",
        "qnaDel",
        "qnaStatus",
        "clientId",
        "roleId",
        "attachmentID"
    FROM qnaOne
    WHERE "qnaOneId" = #{boardId}
    AND "clientId" = #{userId}
    AND ROWNUM = 1
        
    </select>


    <delete id="deleteOneBoard" parameterType="String">
        update qnaOne
        set "qnaDel" = 'Y', 
        "qnaDelDate" = SYSDATE
        where "qnaOneId" = #{boardId}
    </delete>
</mapper>