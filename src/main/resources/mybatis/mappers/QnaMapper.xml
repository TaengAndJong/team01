<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.team01.qna.dao.QnaDao">  <!--사용하는 Dao 연결-->
    <!--메서드 구현부-->
    <!--id로 특정 사용자 데이터 조회-->
    <select id="userQnaDataList" parameterType="String" resultType="QnaVO">
        SELECT qna."qnaId", qnaone."qnaOneId" , qnaone."qnaTitle" , qnaone."qnaContent" , qnaone."qndWriter" , qnaone."qnaDate"
        FROM qna
        JOIN qnaone ON qna."qnaId" = qnaone."qnaId"
        WHERE qna."userId" = #{userId}
    </select>
</mapper>


