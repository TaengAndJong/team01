import React, { useState, useEffect } from 'react';


function AdminDashboard({ setUrl,data }) {


    // // URL 설정
    // useEffect(() => {
    //     setUrl('/api/admin/dashboard'); // Login 컴포넌트 진입 시 URL 변경
    // }, [setUrl]);





    return (
        <>
        대시보드
        </>
    );
}
//hashedPassword 는 json 코드로 서버로 전송됨
export default AdminDashboard;
